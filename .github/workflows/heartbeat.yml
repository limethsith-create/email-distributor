name: sender-heartbeat

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ring the sender
        run: curl -sS -m 30 "https://email-distributor.vercel.app/api/cron/auto-send" || true
        

# Encryption key for storing Gmail credentials securely
# Generate a random 32-character string
ENCRYPTION_KEY=your-random-32-character-secret-key-here

# Optional: Set max emails per account per day (default: 450)
MAX_EMAILS_PER_ACCOUNT=450
